import theme from 'src/styles/theme';

export type ThemeFontSizes = typeof theme.fontSizes;

export type ThemeButton = typeof theme.button;

export type ThemeColors = typeof theme.colors;

export type Theme = typeof theme;
